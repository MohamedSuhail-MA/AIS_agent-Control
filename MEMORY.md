# MEMORY.md: Project Memory & Status Tracker

## 1. Project Overview & Gold Standard Goals
This repository contains the **Enterprise-Grade Zero-Trust Remote Execution Platform**. It is an orchestration layer and Model Context Protocol (MCP) agent designed to securely bridge cloud-hosted Large Language Models (LLMs) with on-premises Windows infrastructure.

**Gold Standard Objectives:**
- **Zero Trust Network:** No inbound ports opened. Agents poll out via mTLS.
- **Cryptographic Guarantee:** Every job payload is signed via Ed25519 (EdDSA) by the orchestrator. Agents drop tampered or unsigned payloads immediately.
- **Hardware Isolation:** Execution bounded by Windows Job Objects (`KILL_ON_JOB_CLOSE`) to prevent orphan process creep.
- **Semantic Guardrails:** PowerShell Constrained Language Mode (CLM) and Windows Defender Application Control (WDAC) block arbitrary Win32 API access.
- **Observability:** OpenTelemetry `traceparent` headers inject end-to-end traceability from LLM prompt to Windows Event Log.

## 2. Current Project Status

### ✅ Completed
- **Phase 1: Foundation (Data Tier & Control Plane)**
  - Express.js Control Plane API built.
  - Simulated PostgreSQL schema with JSONB payload structure.
  - Concurrent `SELECT ... FOR UPDATE SKIP LOCKED` logic implemented via `visible_at` timestamps in `db.ts`.
- **Phase 3: Integration (MCP & LLM Orchestrator Handshake)**
  - MCP `tools/list` and `tools/call` endpoints exposed.
  - Zod schema validation implemented for tool invocation.
- **Phase 4: Cryptographic Security**
  - Ed25519 payload signing integrated using `tweetnacl`.
  - Agent-side signature verification logic implemented.
- **Phase 5: Job History & Enterprise Observability**
  - OpenTelemetry `traceparent` generated by the orchestrator.
  - React (Vite/Tailwind) Control Plane Dashboard created to monitor fleet, queue, and trace IDs.
- **Extensive Test Coverage**: Created 97 exhaustive Vitest test cases across DB concurrency, Cryptography boundary checking, API Endpoints, Edge Agent Logic, CLI Parsing, and Sandbox Guardrails.
- **Documentation**: Developed specific architecture references for the Control Plane and Edge Agent logic.
- **Agent CLI Productionization**: Extracted the agent into a robust `util.parseArgs` CLI (`src/bin/agent.ts`) that builds to a standalone bundle via `esbuild` (`npm run build:agent`).
- **WDAC Integration Testing**: Refactored the core execution block into a decoupled `WindowsSandbox` module and proved safety mechanisms through 7 dedicated Vitest scenarios modeling COM instantiation and reflection blocking.
- **Production Key Exchange & Health Monitoring**: Replaced pre-shared secrets with dynamic mTLS auto-enrolling CA key generation. Implemented a recurring Node background sweeper on the Control Plane to reap dead agent sessions and stale job locks aggressively. (101 Total Tests passing).

### ⏳ Pending / Next Steps
- **LLM Prompt Injection Filters / AI Guardrails**: Introduce an intelligence-layer filter mapping on the orchestrator edge to evaluate structural validity and catch malicious LLM logic bypass attempts before queueing.
- **Agent UI**: Build out the frontend visualizer.

## 3. Core Architectural Rules (DO NOT BREAK)
1. **Never accept unverified payloads:** `verifyJobPayload` must ALWAYS run before executing a command.
2. **Never expose the orchestrator private key:** The agent must only possess the public key.
3. **Never allow inbound agent connections:** Agents must always `POST /poll`.
4. **Never store non-structural payloads:** Data must strictly adhere to the `toolCallSchema` JSON definition.
5. **Workflow Completion Rule:** "Resume/Go" triggers require Code -> Tests -> Full Test Run Success -> Docs Update -> Docs Verify before responding.

## 4. Test Coverage & Bug Tracker
- *Tests are actively being added. No known bugs currently exist. As bugs are discovered, they will be documented here with their corresponding regression test IDs.*
